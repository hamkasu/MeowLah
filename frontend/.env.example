# Public vars (embedded at build time)
NEXT_PUBLIC_API_URL=http://localhost:4000/v1
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key_here
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_or_leave_empty_for_leaflet

# Server-side only (Railway internal networking)
# Set this in production to use Railway's private network for SSR fetches
# API_INTERNAL_URL=http://backend.railway.internal:4000/v1

# Railway sets these automatically
# PORT=3000
# HOSTNAME=0.0.0.0
