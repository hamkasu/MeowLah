[build]
builder = "nixpacks"
buildCommand = "unset NPM_CONFIG_PRODUCTION && npm install && npm run build"

[deploy]
startCommand = "unset NPM_CONFIG_PRODUCTION && HOSTNAME=0.0.0.0 PORT=3000 npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3000
